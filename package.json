{
  "name": "drmaintenance",
  "version": "0.1.0",
  "description": "A compact Dockerized maintenance planner with MariaDB persistence.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mysql2": "^3.11.5"
  },
  "engines": {
    "node": ">=20"
  }
}
